import { SignInPage } from "@/components/features/SignIn";

export default function Home() {
	return (
		<main className="bg-neutral-100 w-full h-full flex items-center justify-center max-w-full min-h-screen">
			<SignInPage />
		</main>
	);
}
