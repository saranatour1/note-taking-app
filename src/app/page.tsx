import { SignIn } from "@/components/features/SignIn";

export default function Home() {
	return (
		<main className="bg-neutral-100 w-full h-full grid items-center justify-center max-w-full min-h-screen">
			<SignIn />
		</main>
	);
}
