import { ForgotPassword } from "@/components/features/ForgotPassword";

export default function page() {
	return (
		<main className="bg-neutral-100 w-full flex items-center justify-center max-w-full min-h-screen">
			<ForgotPassword />
		</main>
	);
}
